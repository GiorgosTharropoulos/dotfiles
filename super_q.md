When you install popos shell, and the default keyboard shortcut for closing windows is Suepr+Q, you have to paste this command in the terminal:

```bash
gsettings set org.gnome.shell.extensions.dash-to-dock hot-keys false
```
